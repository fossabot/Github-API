language: python
python:
  - "3.5"

before_script:
  - chmod +x dummyTest.py

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: tests/dummyTest.py 
